name: Publish pull request

on:
  pull_request_target:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-and-push-image:
    environment: test
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.meta.outputs.version }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
      - name: Checkout PR
        uses: actions/checkout@v2
        with:
          ref: pull/${{ github.event.pull_request.number }}/head
          path: source
      - name: Merge PR
        run: |
          ls -la .
          ls -la source
          rm -rf source/.github
          cp .github source/
          mv source/* .
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ secrets.GHCR_ACCESS_USERNAME }}
          password: ${{ secrets.GHCR_ACCESS_TOKEN }}
      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@v2
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          flavor: |
            latest=false
          tags: |
            type=ref,event=branch
            type=ref,event=pr
            type=semver,pattern={{version}}
      - name: Build Docker image
        uses: docker/build-push-action@v3
        with:
          context: .
          # push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
      - name: dump
        run: |
          echo ${{ steps.meta.outputs.labels }}
          echo ${{ steps.meta.outputs.tags }}
          echo "${{ toJSON(github) }}"
  # depoly:
  #   needs: build-and-push-image
  #   if: ${{ needs.build-and-push-image.result == 'success' }}
  #   uses: ./.github/workflows/deploy.yml
  #   secrets: inherit
  #   with:
  #     version: ${{ needs.build-and-push-image.outputs.version }}
  #     domain: echo-${{ needs.build-and-push-image.outputs.version }}.dev.xuexb.com